# CSV Viewer Application Configuration

# Authentication Configuration
# These can be overridden by environment variables:
# - APP_AUTH_USERNAME
# - APP_AUTH_PASSWORD

app.auth.username=${APP_AUTH_USERNAME:admin}
app.auth.password=${APP_AUTH_PASSWORD:admin}

# Made with Bob